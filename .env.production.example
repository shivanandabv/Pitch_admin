# Copy to .env.production before production build (or set in CI).
# Non-secret — only the public API origin.
VITE_API_BASE_URL=https://pitchxpo-backend.onrender.com
VITE_BASE_PATH=/pitchxpoconclave/admin/
